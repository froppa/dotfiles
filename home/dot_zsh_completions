# ~/.zsh_completions
# Managed by chezmoi
# Sourced from .zshrc

# Completion behavior
zstyle ':completion:*' rehash true
zstyle ':completion:*' menu no
zstyle ':completion:*:descriptions' format '[%d]'
zstyle ':completion:*' matcher-list \
  'm:{a-zA-Z}={A-Za-z}' \
  'r:|[._-]=* r:|=*' \
  'l:|=* r:|=*'

# Keep git branch completion order stable for checkout/switch
zstyle ':completion:*:git-checkout:*' sort false
zstyle ':completion:*:git-switch:*' sort false

# Colors for completion lists
if [[ -n "${LS_COLORS:-}" ]]; then
  zstyle ':completion:*' list-colors "${(s.:.)LS_COLORS}"
fi

# fzf completion/key-bindings come from `fzf --zsh` in .zshrc; fzf-tab is
# loaded as an oh-my-zsh plugin. Only plugin options live here.

# fzf-tab options
zstyle ':fzf-tab:*' switch-group '<' '>'
zstyle ':fzf-tab:*' fzf-flags --bind=up:up,down:down

if (( $+commands[eza] )); then
  zstyle ':fzf-tab:complete:cd:*' fzf-preview 'eza -1 --color=always $realpath'
elif (( $+commands[ls] )); then
  zstyle ':fzf-tab:complete:cd:*' fzf-preview 'ls -la $realpath'
fi

if (( $+commands[kubectl] )); then
  source <(kubectl completion zsh 2>/dev/null)
fi

if (( $+commands[gh] )); then
  source <(gh completion -s zsh 2>/dev/null)
fi
